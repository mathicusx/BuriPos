import { productsFeature } from './products.reducer';

export const {
  selectProducts,
  selectCreateProduct,
  selectLoadingProduct,
  selectLoadingProducts,
  selectProduct,
  selectProductsState,
  selectUpdateProduct,
} = productsFeature;
